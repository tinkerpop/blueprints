package com.tinkerpop.blueprints.odm.impls.couchdb;

import com.tinkerpop.blueprints.odm.Store;

/**
 * @author Marko A. Rodriguez (http://markorodriguez.com)
 */
public class CouchStore {//implements Store {



    public CouchStore(final String hostname, final int port, final String database) {
        //Session s = new Session(hostname, port);
        //Database db = s.getDatabase(database);
    }
}
